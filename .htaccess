RewriteEngine On
RewriteBase /hesabpars/

# اگر فایل یا دایرکتوری وجود نداشت
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# همه درخواست‌ها به index.php هدایت شوند
RewriteRule ^(.*)$ index.php [QSA,L]